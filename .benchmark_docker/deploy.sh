#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "=============================================="
echo "  fsregistration Benchmark Docker Setup"
echo "=============================================="
echo ""
echo "=== Step 1: Build image (~15 min) ==="
echo "docker build -f .benchmark_docker/Dockerfile -t fsbench:latest ."
echo ""
echo "=== Step 2: Build workspace (first time only, ~2 min cached) ==="
echo "docker run --rm -v /path/to/volumeROS/ros_ws:/home/benchmark/ros_ws \\"
echo "  fsbench:latest /usr/local/bin/docker-entrypoint-build.sh"
echo ""
echo "=== Step 3: Run benchmarks ==="
echo "docker run --rm \\"
echo "  -v /path/to/volumeROS/ros_ws:/home/benchmark/ros_ws \\"
echo "  -v /path/to/volumeROS/dataFolder:/data:ro \\"
echo "  -v ./benchmark_results:/volume/results \\"
echo "  fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh soft 8"
echo ""
echo "=== Method assignment (3 machines, 7 methods) ==="
echo ""
echo "Machine 1:"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh soft"
echo ""
echo "Machine 2:"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh fpfh"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh icp"
echo ""
echo "Machine 3:"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh geotransformer"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh regtr"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh hybridpoint"
echo "  docker run --rm -v /data:/data:ro -v /weights:/volume/weights:ro -v ./results:/volume/results fsbench:latest /usr/local/bin/docker-entrypoint-benchmark.sh pointreggpt"
echo ""
echo "=== Weight files needed ==="
echo "Place in mounted /volume/weights/ directory:"
echo "  - regtr-3dmatch-model-best.pth"
echo "  - hybridpoint-3dmatch.tar"
echo "  - predator-indoor.pth"
echo "  (geotransformer weight is included in the repo)"
