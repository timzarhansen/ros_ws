#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "=============================================="
echo "  fsregistration Benchmark Docker Setup"
echo "=============================================="
echo ""
echo "=== Build benchmark image (~35 min) ==="
echo "docker build -f .benchmark_docker/Dockerfile -t fsbench:latest ."
echo ""
echo "=== Run a benchmark ==="
echo "docker run --rm \\"
echo "  -v /path/to/3dmatch-data:/data:ro \\"
echo "  -v /path/to/weights:/volume/weights:ro \\"
echo "  -v ./benchmark_results:/volume/results \\"
echo "  fsbench:latest <method> [num_workers]"
echo ""
echo "=== Example: run SOFT ==="
echo "docker run --rm \\"
echo "  -v /shared/data:/data:ro \\"
echo "  -v /shared/weights:/volume/weights:ro \\"
echo "  -v ./results:/volume/results \\"
echo "  fsbench:latest soft 8"
echo ""
echo "Results: ./results/soft/outfile_soft_*.csv"
echo ""
echo "=== Available methods ==="
echo "  soft, fpfh, icp, geotransformer, regtr, hybridpoint, pointreggpt"
echo ""
echo "=== Weight files needed ==="
echo "  Place in /shared/weights/ directory:"
echo "    - regtr-3dmatch-model-best.pth"
echo "    - hybridpoint-3dmatch.tar"
echo "    - predator-indoor.pth"
echo "  (geotransformer weight is included in the repo)"
